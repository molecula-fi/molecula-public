{
    "name": "@molecula-monorepo/tsconfig-shared",
    "version": "0.15.0",
    "private": true,
    "description": "Shared ts configuration",
    "scripts": {
        "bump-to": "yarn version --no-commit-hooks --no-git-tag-version --new-version",
        "tsc": "tsc",
        "prettier:check": "prettier --check .",
        "prettier:fix": "prettier --write .",
        "eslint:check": "echo workspace does not contain any code",
        "eslint:fix": "echo workspace does not contain any code"
    },
    "dependencies": {
        "typescript": "^5.4.5"
    },
    "devDependencies": {
        "@daochild/tronweb-typescript": "^1.1.2",
        "@molecula-monorepo/prettier-config-shared": "*"
    }
}
