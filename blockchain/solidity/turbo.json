{
    "extends": ["//"],
    "$schema": "../../node_modules/turbo/schema.json",
    "tasks": {
        "compile": {
            "outputs": ["artifacts/**", "cache/**", "typechain-types/**"]
        },
        "slither": {
            "outputs": ["artifacts/**", "cache/**"]
        }
    }
}
